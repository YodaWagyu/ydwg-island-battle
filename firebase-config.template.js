// 📝 Template สำหรับการตั้งค่า Firebase
// วิธีใช้:
// 1. คัดลอกไฟล์นี้และเปลี่ยนชื่อเป็น firebase-config.js
// 2. แทนที่ค่าด้านล่างด้วยข้อมูลจริงจาก Firebase Console
// 3. ไฟล์ firebase-config.js จะไม่ถูกอัพโหลดขึ้น GitHub (ป้องกันโดย .gitignore)

export const firebaseConfig = {
    apiKey: "YOUR_API_KEY_HERE",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
